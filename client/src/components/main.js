import React, { Component } from 'react';
import { store } from '../store';

class Main extends Component {
  constructor(props){
    super(props);
  }

  render() {
    return(

    )
  }
}

export default Main
